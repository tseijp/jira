export default function BoardContainer(props: any) {
        return (
                <section
                        {...props}
                        className="
                                flex
                                flex-col
                                items-center
                                justify-center
                                w-full
                                h-full
                                gap-10
                        "
                />
        )
}
