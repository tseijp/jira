export const DemoCopyButton = (props: any) => {
        return (
                <button
                        {...props}
                        style={{ position: 'absolute', top: 16, right: 16 }}
                >
                        Copy
                </button>
        )
}
