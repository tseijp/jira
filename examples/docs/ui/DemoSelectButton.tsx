export const DemoSelectButton = (props: any) => {
        return (
                <select
                        {...props}
                        style={{ position: 'absolute', top: 40, right: 16 }}
                />
        )
}
